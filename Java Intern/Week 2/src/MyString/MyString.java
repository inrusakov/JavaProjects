    package MyString;

    public interface MyString {
        /**
         * Метод добавляет в конец текущей строки (изначально текущая строка пуста) символ ‘a’ или ‘b’ (случайно).
         */
        public void addLetter();

        /**
         * Метод печатает текущую строку в консоль.
         */
        public void show();
    }
